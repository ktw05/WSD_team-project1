<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.wsd_final_team_project.dao.MemberDAO">

    <select id="getMemberByUsername" parameterType="string"
            resultType="MemberVO">
        SELECT user_id AS userId,
               username,
               password,
               nickname,
               email,
               created_at AS createdAt,
               role
        FROM Member
        WHERE username = #{username}
    </select>

    <insert id="insertMember" parameterType="MemberVO">
        INSERT INTO Member(username, password, nickname, email, created_at, role)
        VALUES (#{username}, #{password}, #{nickname}, #{email}, NOW(), #{role})
    </insert>

    <!-- 닉네임 중복 체크 -->
    <select id="checkNickname" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM Member
        WHERE nickname = #{nickname}
    </select>

    <!-- 회원 전체 목록 -->
    <select id="getAllMembers" resultType="MemberVO">
        SELECT
            user_id    AS userId,
            username,
            password,
            nickname,
            email,
            created_at AS createdAt,
            role
        FROM Member
        ORDER BY user_id DESC
    </select>

    <!-- 회원 강제 삭제(탈퇴) -->
    <delete id="deleteMember" parameterType="int">
        DELETE FROM Member
        WHERE user_id = #{userId}
    </delete>


</mapper>
